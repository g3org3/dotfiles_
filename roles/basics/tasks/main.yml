---
- name: Install basic deps
  ansible.builtin.apt:
    pkg:
      - unzip
      - zip
      - vim
      - curl
      - wget
      - git
      - gcc
      - zsh
      - clangd-12

- name: Create /tmp
  ansible.builtin.file:
    path: /tmp
    state: directory
    mode: "1777"
